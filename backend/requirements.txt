# ==================== FastAPI Web 框架核心依赖 ====================
# 注意：此文件专为 RK3568 开发板设计，已排除 conda 环境中已有的依赖

# Web 框架
fastapi>=0.104.0,<0.120.0

# ASGI 服务器（不包含标准扩展，避免平台相关依赖）
uvicorn>=0.24.0,<0.32.0

# 模板引擎
jinja2>=3.1.2,<3.2.0

# JWT 认证
PyJWT>=2.8.0,<3.0.0

# 文件上传支持
python-multipart>=0.0.6,<0.1.0

# ==================== 以下依赖已在 conda 环境中，无需安装 ====================
# opencv-python==4.12.0.88  ✓ 已安装
# numpy==1.24.4             ✓ 已安装
# rknn-toolkit-lite2==2.3.2 ✓ 已安装（开发板特有，不要覆盖）
